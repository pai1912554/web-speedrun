# Web Speedrun — เรียนสร้างเว็บจาก 0 ให้ทันแข่ง 3 ชั่วโมง

## เปิดยังไง

1. VS Code > File > Open Folder > เลือกโฟลเดอร์ `web-speedrun`
2. ติดตั้งส่วนขยาย **Live Server** (Ctrl+Shift+X ค้นคำว่า Live Server)
3. คลิกขวาที่ `index.html` > **Open with Live Server**

---

## เริ่มที่ไหน: `studio/index.html`

**ห้องปฏิบัติการ (Studio)** คือส่วนหลักของเว็บนี้ ไม่ใช่บทเรียน.
หน้าจอแบ่งสองฝั่ง — **ซ้ายคือใบสั่งงานทีละขั้น ขวาคือกระดานที่แสดงผลทันที**

เครื่องมือบนกระดานขวา:

| เครื่องมือ | ทำอะไร |
|---|---|
| สลับ "ที่กำลังทำ" / "เป้าหมาย" | ดูปลายทางก่อน แล้วกลับมาดูว่าตอนนี้ขาดชั้นไหน |
| **เส้นบอกระยะ** | ทับค่าจริงลงบนแบบ — ช่องไฟกี่พิกเซล ตัวอักษรขนาดเท่าไหร่ สีชื่ออะไร |
| ตาราง **อ่านทีละบรรทัด** | ใต้โค้ดทุกก้อน อธิบายแต่ละบรรทัดว่าคืออะไร ถ้าไม่มีจะพังยังไง |
| ปุ่มมือถือ | สลับดูจอ 390px ได้ทุกขั้น ไม่ต้องรอตรวจตอนท้าย |
| กล่อง "ลองเอง" | ทุกขั้นมีค่าให้ลองแก้แล้วดูว่าพังยังไง — เห็นของพังคือวิธีจำที่ได้ผลที่สุด |

### ห้องปฏิบัติการทั้งหมด

| ห้อง | เวลา | ฝึกอะไร |
|---|---|---|
| `studio/lab-00-first-page.html` | 35 น. | **เริ่มที่นี่ถ้ายังไม่เคยเขียนเว็บ** — สร้างโฟลเดอร์และไฟล์ใน VS Code, พิมพ์บรรทัดแรก, อธิบายทุกบรรทัด, จบที่ปุ่มกดแล้วทำงานจริง |
| `studio/design-eye.html` | 20 น. | **ฝึกตา** — 8 คู่เปรียบเทียบ เนื้อหาเดียวกัน ต่างกันจุดเดียว ดูก่อนแล้วเดา |
| `studio/lab-01-hero.html` | 25 น. | หัวเว็บ: ลำดับสายตา สเกลระยะห่าง สเกลตัวอักษร จุดเน้นเดียว |
| `studio/lab-02-cards.html` | 25 น. | ตารางการ์ด: Grid ที่ responsive เอง กายวิภาคการ์ด สัดส่วนรูป สถานะว่าง |
| `studio/lab-03-nav.html` | 20 น. | แถบเมนู: Flexbox การจัดแนว sticky เมนูมือถือกดเปิดได้จริง |
| `studio/lab-04-form.html` | 22 น. | ฟอร์ม: จัดกลุ่ม ป้ายกำกับ สถานะโฟกัส ข้อความผิดพลาดที่บอกวิธีแก้ |
| `studio/lab-05-dashboard.html` | 28 น. | แดชบอร์ด: ออกแบบข้อมูล สีเชิงความหมาย กราฟ CSS ล้วน ตารางที่กวาดตาอ่านได้ |

**ลำดับที่แนะนำ:** LAB 00 → ฝึกตา → LAB 01 → 02 → 03 → 04 → 05 → ซ้อมจับเวลา (ประกอบ 01+02+03 เป็นหน้าเดียวใน 35 นาที โดยไม่เปิดเฉลย)

---

## ส่วนอื่น

**`lessons/` — เปิดเมื่อสงสัย ไม่ต้องอ่านรวด**
01 โครงสร้าง HTML · 02 CSS layout · 03 JavaScript 6 ท่า · 04 แผนรบ 3 ชม. ·
05 **ตารางแปลโจทย์เป็นเทคนิค** (ได้โจทย์แล้วค้นคำว่าอะไร) · 06 ตั้ง VS Code ให้เร็วขึ้น

**`templates/` — ของสำเร็จรูป ใช้ตอนแข่ง ไม่ใช่ตอนเรียน**
landing · dashboard · crud-table · form · login · gallery · blog · portfolio (ไฟล์เดียวจบทุกอัน)

**`snippets/` — 16 ชิ้นพร้อมปุ่มคัดลอก**
modal · tab · accordion · ค้นหาสด · dark mode · toast · countdown · chart · hamburger · validation

---

## ชุดค่าที่ต้องจำ

```
ระยะห่าง   4 · 8 · 16 · 24 · 40 · 64        ห้ามใช้ค่านอกชุด
ตัวอักษร   48/800 · 24/700 · 16/400 · 13/500
สี         พื้น 1 · ตัวอักษรเข้ม+จาง · เส้น 1 · สีเน้น 1 (ใช้ 2-3 ที่เท่านั้น)
```

## กฎ 5 ข้อ

1. ทำให้ **เห็นภาพ** ก่อนทำให้ **ทำงาน** — กรรมการเห็นหน้าจอก่อนเห็นโค้ด
2. ห้ามติดตั้งอะไรตอนแข่ง — HTML+CSS+JS ล้วน เปิดไฟล์ก็รัน
3. ข้อมูลปลอมก่อนเสมอ — `let items = [...]` แล้ว render
4. ทำงานได้ 1 อย่างสมบูรณ์ ดีกว่า 5 อย่างครึ่งๆ
5. เซฟ + เปิดดูในเบราว์เซอร์ทุก 10 นาที

**นาทีที่ 150 หยุดเพิ่มฟีเจอร์ทุกกรณี** — คนแพ้ส่วนใหญ่แพ้เพราะเพิ่มของนาทีที่ 170 แล้วพังยกเว็บ
